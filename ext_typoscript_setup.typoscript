module.tx_moduledatalisting {
	default {
		settings {
			searchableColumns =
		}
		additionalColumns {
			# table {
			# 	some_column = Label
			# }
		}
	}
	fe_users {
		settings {
			searchableColumns := addToList(fe_users.first_name,fe_users.last_name,fe_users.username,fe_users.email,fe_users.uid)
		}
	}
}
